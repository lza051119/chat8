# Chat8 服务端网络配置
# 当前配置的服务器IP: 10.21.166.204
# 服务端口: 8000

# 启动命令:
# cd chat8-server/backend
# python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 防火墙配置 (Windows):
# netsh advfirewall firewall add rule name="Chat8 Server" dir=in action=allow protocol=TCP localport=8000

# 防火墙配置 (Linux):
# sudo ufw allow 8000
